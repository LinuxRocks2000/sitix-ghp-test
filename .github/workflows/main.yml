on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          name: LinuxRocks2000/sitix-rewrite
          ref: master
      - name: Sitix build
        run: |
             echo "hi"
             ls -la
